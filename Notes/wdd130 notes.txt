Request repsonse cycle, client asks the web server responds with respinse code ex: 200 OK, 404 Not found
when webpage jumps that is due to the page loading an image without knowing its size
<html> <head> </head> <body> </body> </html>
HTML is structure CSS is styles
<!-- this is how you commment on HTML -->
<h1>heading</h1>
<p> paragraph </p>
<h2>subheading</h2>
<h1> font is going to be slightly larger than <h2> font by default
<head> is not meant to be seen and is meta data, can be used to tell the browser how to render the page
<body>
    <header>
    semantic tag to establish the header, typically the same page to page
        <nav>
            contains navigation menu in the header
            <a tag is short for anchor, it is used to send to and reference other pages /a>
            <a href=example.html >Home<(this is how to embed the link, whatever text is in >< is the name rendered for the link)
         </nav> 
    
    <header>
    <main>


    </main>
    <footer>
    <hr> (used to create a horizontal line diveder on the page)
    <p>&copy(shortcut for copyright symbol)